from django.urls import path, re_path
# from nasrinjafari import views

# urlpatterns = [
#     path('', views.nas_index, name='index'),
# ]

app_name = 'nasrin'
# urlpatterns = [
#     path('', views.index, name='index'),
#     # path('<int:conversationID>/', views.conversation, name='conversation'),
#     re_path(r'(?P<conversationID>\w+)/', views.conversation, name='conversation'),
# ]
